name: Daily GPT Update

on:
  schedule:
    - cron: '0 13 * * *'  # 8am CST
  workflow_dispatch:

jobs:
  update:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Install dependencies
        run: npm install

      - name: Run update script
        run: node update.js
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}

      - name: Read historical-news.html content
        id: read_html
        run: |
          content=$(base64 -w 0 historical-news.html)
          echo "content=$content" >> $GITHUB_OUTPUT

      - name: Commit file via GitHub API
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const path = 'historical-news.html';
            const content = `${{ steps.read_html.outputs.content }}`;

            const { data: fileData } = await github.repos.getContent({
              owner: context.repo.owner,
              repo: context.repo.repo,
              path
            }).catch(() => ({ data: {} }));

            await github.repos.createOrUpdateFileContents({
              owner: